---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: weaveruns.weave.fusion-platform.io
spec:
  group: weave.fusion-platform.io
  names:
    kind: WeaveRun
    listKind: WeaveRunList
    plural: weaveruns
    shortNames:
    - fr
    singular: weaverun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.chainRef.name
      name: Chain
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.startTime
      name: Start
      type: date
    - jsonPath: .status.completionTime
      name: End
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WeaveRun represents one live execution of a WeaveChain.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WeaveRunSpec defines the immutable parameters of one chain
              execution.
            properties:
              chainRef:
                description: ChainRef is the WeaveChain this run executes. Immutable
                  after creation.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              parameterOverrides:
                description: |-
                  ParameterOverrides are injected as extra environment variables into all
                  steps of this run. Useful for passing runtime context from webhook payloads.
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: Name of the environment variable. Must be a C_IDENTIFIER.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              triggerRef:
                description: TriggerRef identifies the WeaveTrigger that created this
                  run, if any.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - chainRef
            type: object
          status:
            description: WeaveRunStatus reflects the live execution state of the run.
            properties:
              completionTime:
                description: CompletionTime is when the run reached a terminal phase.
                format: date-time
                type: string
              message:
                description: Message holds a human-readable summary of the current
                  state.
                type: string
              phase:
                description: Phase is the overall lifecycle phase of this run.
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                - Stopped
                type: string
              sharedPVCName:
                description: |-
                  SharedPVCName is the name of the per-run shared PVC, set once the PVC has
                  been successfully created. Empty when the chain has no SharedStorage config.
                type: string
              startTime:
                description: StartTime is when the run moved from Pending to Running.
                format: date-time
                type: string
              steps:
                description: Steps holds the per-step execution state, indexed by
                  step name.
                items:
                  description: WeaveRunStepStatus tracks the execution state of one
                    DAG step.
                  properties:
                    completionTime:
                      description: CompletionTime is when the step reached a terminal
                        phase.
                      format: date-time
                      type: string
                    deploymentRef:
                      description: |-
                        DeploymentRef is the name of the apps/v1 Deployment created for Deploy-kind steps.
                        Nil for Job-kind steps.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    jobRef:
                      description: JobRef is the name of the batch/v1 Job created
                        for this step.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    message:
                      description: Message holds a human-readable status detail or
                        failure reason.
                      type: string
                    name:
                      description: Name matches the WeaveChainStep name.
                      type: string
                    nextRetryAfter:
                      description: NextRetryAfter is the earliest time the step will
                        be retried.
                      format: date-time
                      type: string
                    outputCaptured:
                      description: |-
                        OutputCaptured is true once the step's JSON stdout has been successfully
                        written to the run's output ConfigMap. Only meaningful when the step's
                        WeaveChainStep has ProducesOutput: true.
                      type: boolean
                    phase:
                      description: Phase is the current lifecycle phase of this step.
                      enum:
                      - Pending
                      - Running
                      - Succeeded
                      - Failed
                      - Skipped
                      - Retrying
                      type: string
                    retryCount:
                      description: RetryCount tracks how many times this step has
                        been retried.
                      format: int32
                      type: integer
                    startTime:
                      description: StartTime is when the batch Job was first submitted.
                      format: date-time
                      type: string
                  required:
                  - name
                  - phase
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
